# Push Swap

This project implements the push_swap algorithm, which is a sorting algorithm that uses two stacks. The goal of the project is to sort a list of integers using the least amount of operations possible.  

## Usage

To compile the project, use the Makefile provided.  

## Requirements

- Make  
- gcc  

## Compilation

To compile the project, simply run:
```
make
```
